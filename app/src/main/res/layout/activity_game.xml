<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root_game_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/tv_game_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            android:text="¡PAPA CALIENTE!"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="#D32F2F"/>

        <!-- Grid of players -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_players"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
            app:spanCount="2"
            android:clipToPadding="false"
            android:padding="8dp"/>

        <!-- Request button, visible when not having the potato -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_request_potato"
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:text="¡SOLICITAR PAPA!"
            android:textSize="22sp"
            android:textStyle="bold"
            app:backgroundTint="#FF9800"
            android:textColor="@android:color/white"
            android:layout_margin="16dp"
            app:cornerRadius="16dp"
            android:visibility="visible"
            app:elevation="8dp"/>

    </LinearLayout>

    <!-- Red flash overlay for final 10 seconds -->
    <View
        android:id="@+id/view_urgent_flash"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#88FF0000"
        android:visibility="gone" />

</FrameLayout>
